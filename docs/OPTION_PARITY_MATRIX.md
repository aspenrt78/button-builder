# Option Parity Matrix

Generated from side-panel update fields in `src/components/ConfigPanel.tsx`.

- Panel options audited: **157**
- Preview + YAML: **84**
- YAML only: **73**
- Missing YAML: **0**

| Option | Status | Notes |
|---|---|---|
| `alwaysAnimateCard` | Preview + YAML |  |
| `alwaysAnimateIcon` | Preview + YAML |  |
| `aspectRatio` | Preview + YAML |  |
| `backdropBlur` | Preview + YAML |  |
| `backgroundColor` | Preview + YAML |  |
| `backgroundColorOpacity` | Preview + YAML |  |
| `borderColor` | Preview + YAML |  |
| `borderColorAuto` | Preview + YAML |  |
| `borderRadius` | Preview + YAML |  |
| `borderStyle` | Preview + YAML |  |
| `borderWidth` | Preview + YAML |  |
| `cardAnimation` | Preview + YAML |  |
| `cardAnimationSpeed` | Preview + YAML |  |
| `cardOpacity` | Preview + YAML |  |
| `cardSize` | YAML only | Dashboard/layout rendering detail; exported to YAML |
| `color` | Preview + YAML |  |
| `colorAuto` | Preview + YAML |  |
| `colorType` | Preview + YAML |  |
| `conditionalEntity` | YAML only | Update/visibility conditions are runtime-only in HA |
| `conditionalOperator` | YAML only | Update/visibility conditions are runtime-only in HA |
| `conditionalState` | YAML only | Update/visibility conditions are runtime-only in HA |
| `confirmation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `customFields` | Preview + YAML |  |
| `customFontName` | Preview + YAML |  |
| `customFontUrl` | Preview + YAML |  |
| `customGridEnabled` | Preview + YAML |  |
| `customGridTemplateAreas` | Preview + YAML |  |
| `customGridTemplateColumns` | Preview + YAML |  |
| `customGridTemplateRows` | Preview + YAML |  |
| `disableKeyboard` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `doubleTapAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `doubleTapActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `doubleTapActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `doubleTapActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `doubleTapActionSound` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `entity` | Preview + YAML |  |
| `entityPicture` | Preview + YAML |  |
| `entityPictureStyles` | Preview + YAML |  |
| `extraStyles` | Preview + YAML |  |
| `fontFamily` | Preview + YAML |  |
| `fontSize` | Preview + YAML |  |
| `fontWeight` | Preview + YAML |  |
| `gradientAngle` | Preview + YAML |  |
| `gradientColor1` | Preview + YAML |  |
| `gradientColor2` | Preview + YAML |  |
| `gradientColor3` | Preview + YAML |  |
| `gradientColor3Enabled` | Preview + YAML |  |
| `gradientEnabled` | Preview + YAML |  |
| `gradientOpacity` | Preview + YAML |  |
| `gradientType` | Preview + YAML |  |
| `gridColumns` | YAML only | Dashboard/layout rendering detail; exported to YAML |
| `gridRows` | YAML only | Dashboard/layout rendering detail; exported to YAML |
| `gridStyles` | Preview + YAML |  |
| `groupExpand` | YAML only | Not currently simulated in preview |
| `hapticFeedback` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `height` | Preview + YAML |  |
| `hidden` | Preview + YAML |  |
| `hiddenTemplate` | YAML only | Template logic executes in HA, not local preview |
| `holdAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionRepeat` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionRepeatLimit` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdActionSound` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `holdTime` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `icon` | Preview + YAML |  |
| `iconAnimation` | Preview + YAML |  |
| `iconAnimationSpeed` | Preview + YAML |  |
| `iconColor` | Preview + YAML |  |
| `iconColorAuto` | Preview + YAML |  |
| `iconDoubleTapAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconDoubleTapActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconDoubleTapActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconDoubleTapActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconHoldAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconHoldActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconHoldActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconHoldActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconPressAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconPressActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconPressActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconReleaseAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconReleaseActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconReleaseActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconTapAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconTapActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconTapActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconTapActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `iconTemplate` | YAML only | Template logic executes in HA, not local preview |
| `imgCellStyles` | Preview + YAML |  |
| `label` | Preview + YAML |  |
| `labelAttribute` | Preview + YAML |  |
| `labelColor` | Preview + YAML |  |
| `labelColorAuto` | Preview + YAML |  |
| `labelEntity` | Preview + YAML |  |
| `labelTemplate` | YAML only | Template logic executes in HA, not local preview |
| `layout` | Preview + YAML |  |
| `letterSpacing` | Preview + YAML |  |
| `lineHeight` | Preview + YAML |  |
| `liveStreamAspectRatio` | YAML only | Live stream rendering not simulated in preview |
| `liveStreamFitMode` | YAML only | Live stream rendering not simulated in preview |
| `lock` | Preview + YAML |  |
| `lockStyles` | Preview + YAML |  |
| `name` | Preview + YAML |  |
| `nameColor` | Preview + YAML |  |
| `nameColorAuto` | Preview + YAML |  |
| `nameTemplate` | YAML only | Template logic executes in HA, not local preview |
| `numericPrecision` | YAML only | Dashboard/layout rendering detail; exported to YAML |
| `padding` | Preview + YAML |  |
| `pressAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `pressActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `pressActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `pressActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `protect` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `releaseAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `releaseActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `releaseActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `releaseActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `sectionMode` | YAML only | Dashboard/layout rendering detail; exported to YAML |
| `shadowColor` | Preview + YAML |  |
| `shadowOpacity` | Preview + YAML |  |
| `shadowSize` | Preview + YAML |  |
| `showEntityPicture` | Preview + YAML |  |
| `showIcon` | Preview + YAML |  |
| `showLabel` | Preview + YAML |  |
| `showLastChanged` | Preview + YAML |  |
| `showLiveStream` | YAML only | Live stream rendering not simulated in preview |
| `showName` | Preview + YAML |  |
| `showState` | Preview + YAML |  |
| `showUnits` | Preview + YAML |  |
| `size` | Preview + YAML |  |
| `spin` | Preview + YAML |  |
| `spinDuration` | YAML only | Runtime/state-template behavior in HA |
| `spinner` | Preview + YAML |  |
| `spinnerTemplate` | YAML only | Template logic executes in HA, not local preview |
| `stateColor` | Preview + YAML |  |
| `stateColorAuto` | Preview + YAML |  |
| `stateDisplay` | Preview + YAML |  |
| `stateDisplayTemplate` | YAML only | Template logic executes in HA, not local preview |
| `stateStyles` | Preview + YAML |  |
| `tapAction` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `tapActionData` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `tapActionJavascript` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `tapActionNavigation` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `tapActionSound` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `tapActionToast` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `template` | YAML only | Template logic executes in HA, not local preview |
| `textTransform` | Preview + YAML |  |
| `thresholdColor` | Preview + YAML |  |
| `tooltip` | Preview + YAML |  |
| `tooltipStyles` | Preview + YAML |  |
| `triggerEntity` | YAML only | Update/visibility conditions are runtime-only in HA |
| `triggersUpdate` | YAML only | Update/visibility conditions are runtime-only in HA |
| `units` | Preview + YAML |  |
| `updateTimer` | YAML only | Interaction/runtime behavior; YAML only in preview |
| `variables` | YAML only | Template logic executes in HA, not local preview |
